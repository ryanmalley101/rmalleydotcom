{
    "name": "roll20-bridge",
    "version": "1.0.0",
    "private": true,
    "description": "Browser extension: mirrors the official Roll20 Cypher System sheet into the tabletop app.",
    "scripts": {
        "build": "esbuild options-src.js --bundle --outfile=options.bundle.js --target=chrome110"
    },
    "devDependencies": {
        "esbuild": "^0.23.1"
    },
    "dependencies": {
        "amazon-cognito-identity-js": "^6.3.12"
    }
}
